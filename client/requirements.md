## Packages
date-fns | Formatting dates for the last service date and created at
lucide-react | Beautiful icons for the UI and low balance alerts
react-hook-form | Form state management
@hookform/resolvers | Zod validation resolver for the forms

## Notes
The backend provides a computed `isLowBalance` field for vehicles.
Fastag recharge uses POST /api/vehicles/:id/recharge.
